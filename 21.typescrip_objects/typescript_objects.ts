// write a program that creayes objects containing these items.

// detatyp of person object.
interface person {
    age: number,
    name: string,
    nationality :string,
    student : boolean,
}
//person object.
let person:person  = {
    age :19 ,
    name : 'moiz',
    nationality : 'pakistan' ,
    student : true
};

// print person.
console.log(person);

// detatyp of car object
interface car {
    maker: string,
    color: string,
    nationality :string,
    automatic: boolean,
}

// car object.
let car = {
    maker : 'toyota',
    color : 'black',
    automatic : true
}

// print car.

 console.log(car);
 